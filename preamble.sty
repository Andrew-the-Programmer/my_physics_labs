\usepackage[a4paper,top=1.3cm,bottom=2cm,left=1cm,right=1.5cm,marginparwidth=0.75cm]{geometry}

%%% Работа с языком
\usepackage{cmap}					% поиск в PDF
\usepackage{mathtext} 				% русские буквы в фомулах
\usepackage[T2A]{fontenc}			% кодировка
\usepackage[utf8]{inputenc}			% кодировка исходного текста
\usepackage[english,russian]{babel}	% локализация и переносы
\usepackage{multirow}

%%% Работа с документом
\usepackage{graphicx}

\usepackage{wrapfig}
\usepackage{tabularx}

\usepackage{hyperref}
\usepackage[rgb]{xcolor}
\hypersetup{
colorlinks=true,urlcolor=blue
}
%%% tables
% \usepackage{enumitem} %% for effortless customization of enumeration (windows)
% \usepackage{adjustbox} (windows)
\usepackage{tabularx}

%%% enumerate
\usepackage{enumitem} % for various \item values

\usepackage{textcomp, gensymb}

%%% Дополнительная работа с математикой
\usepackage{amsmath, amsfonts, amssymb, amsthm, mathtools} % AMS
\usepackage{icomma} % "Умная" запятая: $0,2$ --- число, $0, 2$ --- перечисление
\usepackage{xparse}

\usepackage{pgfplots}
\pgfplotsset{width=8cm,compat=1.9}
\usepgfplotslibrary{external}
\tikzexternalize

%%% Has to be after amsmath
% \usepackage{cleveref} %% for clever referencing (windows)
%%% ----------------------

%% Номера формул
% \mathtoolsset{showonlyrefs=true} % Показывать номера только у тех формул, на которые есть \eqref{} в тексте.

%% Fonts
\usepackage{euscript} % Шрифт Евклид
\usepackage{mathrsfs} % Красивый матшрифт
\usepackage{esint}
% \usepackage{tempora}  % Times for numbers in math mode
% % \usepackage{newtxmath}
% \usepackage{newtxtext}

%% Свои команды
% \renewcommand{\phi}{\varphi}
% \renewcommand{\epsilon}{\varepsilon}

\DeclareMathOperator{\sgn}{\mathop{sgn}}
\renewcommand{\vec}[1]{\overrightarrow{#1}}
\newcommand{\D}[2][\empty]{\Delta^{#1} #2}
\renewcommand{\d}[2][\empty]{\mathrm{d}^{#1} #2}
\newcommand{\dd}[2][\empty]{\partial^{#1} #2}
\newcommand{\deriv}[3][\empty]{\frac{\d[#1]{#2}}{\d[#1]{#3}}}
\newcommand{\partderiv}[3][\empty]{\frac{\dd[#1]{#2}}{\dd[#1]{#3}}}
\newcommand{\average}[1]{\overline{#1}}
\newcommand{\refb}[1]{$\left( \ref{#1} \right)$}

%% Перенос знаков в формулах (по Львовскому)
\newcommand*{\hm}[1]{#1\nobreak\discretionary{}
{\hbox{$\mathsurround=0pt #1$}}{}}

\newcommand {\Title} [3] {
\begin{titlepage}
	\begin{center}
	{\large МОСКОВСКИЙ ФИЗИКО-ТЕХНИЧЕСКИЙ ИНСТИТУТ (НАЦИОНАЛЬНЫЙ ИССЛЕДОВАТЕЛЬСКИЙ УНИВЕРСИТЕТ)}
	\end{center}
	\begin{center}
	{\large Физтех-школа прикладной математики и информатики}
	\end{center}
	
	\vspace{4.5cm}
	{\large
	\begin{center}
	{\bf #1 #2}\\
		#3
	\end{center}
	}	
	\vspace{2cm}
	\begin{flushright}
	{\large Автор:\\ Чикин Андрей Павлович \\
	\vspace{0.2cm}
	Б05-304}
	\end{flushright}
	\vspace{8cm}
	\begin{center}
	Долгопрудный, 2023
	\end{center}
\end{titlepage}
}

\date{\today}